<?php

    // $busqueda = isset($_POST['busqueda']) ? $ruta->real_escape_string($_POST['busqueda']): null;
    // $sql = $ruta->query("SELECT * FROM productos WHERE cod_pro LIKE '%$busqueda%' OR nombre LIKE '%$busqueda%' OR precio LIKE '%$busqueda%'");

    // $num_rows = $sql-> num_rows;

    // if($num_rows > 0){
    //     while ($row = $sql->fetch_array()){
    //        echo '<div class="pro">' . $row ['cod_pro'].' - '.$row['nombre'].' - $'.$row['precio'].'</div>';
    //     }
    // }else{
    //     echo "Producto Fuera de Stock";
    // }
    // echo json_encode( JSON_UNESCAPED_UNICODE);
    

?>